# deps
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# env files — NEVER commit
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
.vercel

# deploy.sh SÍ se versiona: no contiene secretos (las env vars viven en el
# dashboard de Vercel) y su versión retirada debe estar en el repo.

# typescript
*.tsbuildinfo
next-env.d.ts
.env*
.env.local.backup

# generado por el prebuild — no versionar (ensucia git status)
public/pdf.worker.min.mjs

# El PDF se genera con scripts/gen-manual.py — no versionar el binario
Manual-BRUTAL-IA.pdf
